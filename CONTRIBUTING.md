# Contributing

1. Create a branch from `main`.
2. Keep deployment-specific values in ignored local configuration.
3. Run `npm run check` before opening a pull request.
4. Include tests for configuration or server behavior changes.
5. Describe any new shell command or write-capable endpoint in the pull request.

Do not commit API keys, SSH keys, bearer tokens, local model paths, benchmark databases, or private network addresses.
